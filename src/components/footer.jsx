import React from 'react';
import styled from 'styled-components/macro';

const IndexNax = styled.div`
  width: 100%;
  text-align: center;
`;

const Footer = (props) => <IndexNax id="indexNav">Footer</IndexNax>;

export default Footer;
